#include <stdio.h>
int main()
{
    // // If 1 int reserver 4 bytes in memory then array arr will reserve 3 * 4 = 12 bytes in memory.Each integer will reserve 4 bytes.
    // int arr[3] = {1, 2, 3};
    // printf("The value of arr[0] is %d\n", arr[0]);
    // printf("The value of arr[1] is %d\n", arr[1]);
    // printf("The value of arr[2] is %d\n", arr[2]);

    // If 1 float reserver 4 bytes in memory then array arr will reserve 3 * 4 = 12 bytes in memory.Each float will reserve 4 bytes.
    // float arr[3] = {1, 2, 3};
    // printf("The value of arr[0] is %f\n", arr[0]);
    // printf("The value of arr[1] is %f\n", arr[1]);
    // printf("The value of arr[2] is %f\n", arr[2]);

    // If 1 char reserver 1 byte in memory then array arr will reserve 3 * 1 = 3 bytes in memory.Each float will reserve 1 byte.
    // char arr[3] = {'1', '2', '3'};
    // printf("The value of arr[0] is %c\n", arr[0]);
    // printf("The value of arr[1] is %c\n", arr[1]);
    // printf("The value of arr[2] is %c\n", arr[2]);

    return 0;
}